## Basically two separate scripts that transfer files from a SFTP (pass=key) to a VDS amd then save them on am S3.

# Other script is looking through these files looking for an ID that he gets from a web-service through the groc route.

A file searcher template, easily working with k8s/k3s.
